---- Tasks ----
(In March) Replace os/os2 to os on the next Odin upgrade. That also includes it's use in main.odin

(In March) Remove the os package use in values.odin and the strings.upper function. Use as in the original code instead. Add API implement todo for Upper, NextFiles, FileType, ReadDir, CloseDir, and CurrentDir.
Use the blitz functions Left and Right instead of slices.

Add private="file" to functions only used in a file.

Promos.bb:
There are 17 functions. One is ~500 lines, the other is ~4100 lines, and the rest are small ~20 lines each.
DisplayPromo will be split into sections similar to how I did other files. I'll do a fake scope around the sections and then paste them to promo.odin as I reach the 300 line mark in temp.odin.



